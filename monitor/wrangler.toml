name = "smoltbot-monitor"
main = "src/index.ts"
compatibility_date = "2024-01-01"

[triggers]
crons = ["*/5 * * * *"]

# Secrets (set via wrangler secret put):
# MONITOR_SECRET      (shared secret for HTTP trigger endpoint)
